// Enrollment client removed — placeholder to avoid 404s if referenced
document.addEventListener('DOMContentLoaded', () => {
  const s = document.getElementById('status')
  if (s) s.textContent = 'Face enrollment feature has been removed.'
})
