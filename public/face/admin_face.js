// Face recognition client removed — placeholder to avoid 404 if script still referenced.
document.addEventListener('DOMContentLoaded', () => {
  const r = document.getElementById('result')
  if (r) r.textContent = 'Face recognition feature removed by admin.'
})
